---
import Layout from '../layouts/Layout.astro';
const posts = await Astro.glob('./posts/*.md');
---

<Layout>
	<h1>Tátův a můj blog</h1>
	<ul>
		{posts.map((post) => (
			<li>
				<a href={post.url}>{post.frontmatter.title}</a>
				- {new Date(post.frontmatter.date).toLocaleDateString('en-GB')}
			</li>
		))}
	</ul>
</Layout>
